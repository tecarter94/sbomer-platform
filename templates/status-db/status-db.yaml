{{- if .Values.global.includeStatusDb }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-status-db
  labels:
    app: status-db
    release: {{ .Release.Name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: status-db
  template:
    metadata:
      labels:
        app.kubernetes.io/name: status-db
    spec:
      containers:
        - name: postgresql
          image: {{ .Values.infra.statusDb.image }}
          ports:
            - containerPort: 5432
              name: postgres
          env:
            - name: POSTGRESQL_DATABASE
              value: {{ .Values.infra.statusDb.env.database | quote }}
            - name: POSTGRESQL_USER
              value: {{ .Values.infra.statusDb.env.user | quote }}
            - name: POSTGRESQL_PASSWORD
              value: {{ .Values.infra.statusDb.env.password | quote }}
          # Use ephemeral storage for local dev
          volumeMounts:
            - name: db-data
              mountPath: /var/lib/pgsql/data
      volumes:
        - name: db-data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: status-db
  labels:
    app.kubernetes.io/name: status-db
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.infra.statusDb.service.port }}
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    app.kubernetes.io/name: status-db
{{- end }}